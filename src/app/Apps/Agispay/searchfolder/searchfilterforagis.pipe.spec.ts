import { SearchfilterforagisPipe } from './searchfilterforagis.pipe';

describe('SearchfilterforagisPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchfilterforagisPipe();
    expect(pipe).toBeTruthy();
  });
});
