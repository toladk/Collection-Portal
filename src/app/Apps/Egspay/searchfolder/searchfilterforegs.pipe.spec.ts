import { SearchfilterforegsPipe } from './searchfilterforegs.pipe';

describe('SearchfilterforegsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchfilterforegsPipe();
    expect(pipe).toBeTruthy();
  });
});
