import { SearchfilterforarenaPipe } from './searchfilterforarena.pipe';

describe('SearchfilterforarenaPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchfilterforarenaPipe();
    expect(pipe).toBeTruthy();
  });
});
