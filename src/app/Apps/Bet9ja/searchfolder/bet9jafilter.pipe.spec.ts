import { Bet9jafilterPipe } from './bet9jafilter.pipe';

describe('Bet9jafilterPipe', () => {
  it('create an instance', () => {
    const pipe = new Bet9jafilterPipe();
    expect(pipe).toBeTruthy();
  });
});
